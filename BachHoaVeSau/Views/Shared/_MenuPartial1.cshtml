@model IEnumerable<BachHoaVeSau.LOAISANPHAM>

<div class="col-md-2 menu">
    <div class="position-sticky" style="top:5rem;">
        <div class="navbar navbar-expand-lg navbar-light ">
            <button class="navbar-toggler " type="button" data-bs-toggle="collapse"
                    data-bs-target="#navbarSupported" aria-controls="navbarSupported" aria-expanded="false"
                    aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupported">
                <div>
                    <button type="button" class=" btn btn-success">Danh mục sản phẩm</button>
                    <ul class="list-unstyled ps-0">

                        <li class="mb-1">
                            <button class="btn align-items-center">
                                @foreach (var item in Model)
                                {

                                <li>
                                    
                                    @Html.ActionLink(@item.TENLSP, "SanPhamTheoLoai", "Home");
                                </li>
                        }
                                </button>
                                </li>

                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>

